# SPDX-License-Identifier: BSD-3-Clause
---
version: '3.8'

services:
  organic-typing-app:
    build: .
    ports:
    - "3000:3000"
    environment:
    - GITHUB_TOKEN=${GITHUB_TOKEN}
    - WEBHOOK_SECRET=${WEBHOOK_SECRET}
    - PORT=3000
